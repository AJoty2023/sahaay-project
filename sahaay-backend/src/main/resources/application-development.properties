# ============== application-development.properties ==============
# Override database configuration for development
spring.datasource.url=jdbc:h2:mem:devdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;CASE_INSENSITIVE_IDENTIFIERS=TRUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Override JPA Configuration for Development
spring.jpa.hibernate.ddl-auto=create-drop
<<<<<<< HEAD
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
=======
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
>>>>>>> 20b0075 (all frontend)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Disable Flyway for development (since we're using create-drop)
spring.flyway.enabled=false

# Development specific logging
logging.level.root=INFO
logging.level.com.example.backend=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Disable external services for development
app.notification.email.enabled=false
app.notification.sms.enabled=false

# Override HikariCP settings for H2
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2